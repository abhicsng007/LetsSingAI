# Frontend

Vite + React app for LetsSingAI. The product README, architecture, and hackathon notes live in the [repo root](../README.md).

```bash
npm install
npm run dev        # http://localhost:5173
```

The UI talks to the FastAPI coach at `http://localhost:8000` unless you set `VITE_API_BASE`.
